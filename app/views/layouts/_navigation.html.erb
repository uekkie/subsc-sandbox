<nav class="navbar is-dark" role="navigation" aria-label="main navigation">
      <div class="navbar-brand">
        <%= link_to root_path, class:"navbar-item" do %>
          <h1 class="title is-5 has-text-white"><%= Rails.configuration.application_name %></h1>
        <% end  %>
        <div class="navbar-burger burger" data-target="navbar">
          <span></span>
          <span></span>
          <span></span>
        </div>
      </div>

      <div id="navbar" class="navbar-menu">
        <div class="navbar-start">
          <% if subscribed? %>
            <div class="navbar-item">
              <%= link_to library_index_path, class: 'navbar-item button is-dark' do %>
                <i class="fa fa-user"></i> &nbsp;&nbsp; マイパスポート
              <% end %>
            </div>
          <% end %>
        </div>
        <div class="navbar-end">
          <div class="navbar-item">
            <% if admin? %>
              <%= link_to '新規作成', new_book_path, class:'button is-dark' %>
            <% end %>
            <div class="field is-grouped">
            <% if user_signed_in? %>

              <div class="navbar-item has-dropdown is-hoverable">
                <%= link_to 'ユーザー情報', edit_user_registration_path, class: "navbar-link" %>
                <div class="navbar-dropdown is-right">
                  <%= link_to edit_user_registration_path, class:"navbar-item" do %>
                    <%= current_user.name %> <% if admin? %> &nbsp; <span class="tag is-warning">管理者</span> <% end %>
                  <% end %>
                  <%= link_to "ログアウト", destroy_user_session_path, method: :delete, class:"navbar-item" %>
                </div>
              </div>
            <% else %>
            <p class="control">
              <%= link_to '料金', pricing_index_path, class: 'navbar-item button is-dark' %>
            </p>
            <p class="control">
              <%= link_to "ログイン", new_user_session_path, class:"navbar-item button is-dark" %>
            </p>
            <p class="control">
              <%= link_to "ユーザー登録", new_user_registration_path, class:"navbar-item button is-dark"%>
            </p>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </nav>
